open editor by type : vim "filename"
after save the file.
git add 'filename'
git commit -m "give msg"
git remote add origin give path
if you got any error 
 do these git fetch origin master
 and 
 git merge origin/master
 git push origin master
